# Single source of truth for app versioning.
# Bumped by scripts/release-pipeline.sh (or manually).
# NOTE: keep metadata/ai.chemistry_learning_org.yml (F-Droid) in sync.
versionCode=4
versionName=1.0.3
